{
  "name": "nexalayer",
  "version": "0.1.0",
  "description": "NexaLayer SDK — The Network Execution Layer for AI Agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js 2>/dev/null || echo 'Add jest for tests'",
    "lint": "echo 'Add eslint'"
  },
  "keywords": ["proxy", "agent", "session", "nexalayer"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NexaLayer/nexalayer-sdk"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
